
version: 0.1

phases:   
  build:
    commands:
      - echo "Hello World"
      - export key='sikandar-keypair-ohio'
      
      
artifacts:
  files:
    - cf_params.json
  name: artifact-name
  discard-paths: yes
  base-directory: location      
